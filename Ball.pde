int xMove = 1, yMove = 1;

void ball() {
  ballStart();
  ballMove();
}

void ballMove(){
  if (xBall <= (height*0) + (ballDiame
  xBall += move;
  yBall += move;
}

void ballStart(){
  ellipse(xBall, yBall, ballDiameter, ballDiameter);
}
